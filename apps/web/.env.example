# Base URL shown for webhook endpoints (and in API usage examples).
VITE_WEBHOOK_BASE_URL=https://hooks.procd.cc
# Dev only: where the Vite dev server proxies /api (defaults to the local worker).
# VITE_API_PROXY_TARGET=http://localhost:8787
# Absolute site origin for Open Graph tags; defaults to Vercel's production URL at build time.
# VITE_SITE_URL=https://webhooks.procd.cc
