{
	"name": "webhook-tester-monorepo",
	"private": true,
	"description": "Webhook Tester: Cloudflare Worker API (apps/api) and React dashboard (apps/web).",
	"scripts": {
		"install:all": "npm --prefix apps/api install && npm --prefix apps/web install",
		"dev:api": "npm --prefix apps/api run dev",
		"dev:web": "npm --prefix apps/web run dev",
		"build:web": "npm --prefix apps/web run build",
		"lint:web": "npm --prefix apps/web run lint",
		"deploy:api": "npm --prefix apps/api run deploy",
		"db:migrate:local": "npm --prefix apps/api run db:migrate:local",
		"db:migrate:remote": "npm --prefix apps/api run db:migrate:remote"
	}
}
